echo "--build start--"

stage('Checkout Stage') {
	echo "--Checkout--"
}

stage('Build Stage') {
	echo "--Build Stage--"
	sh 'npm install'
}

stage('Push Stage') {
	echo "--PUsh Stage--"
	sh 'pm2 start'
}
